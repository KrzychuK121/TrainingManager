spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
      generate_statistics: true
  flyway:
    enabled: true
logging.level.org.hibernate.orm.jdbc.bind: TRACE